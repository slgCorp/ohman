<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginLeft="@dimen/activity_vertical_margin"
    android:layout_marginRight="@dimen/activity_vertical_margin">

    <LinearLayout
        android:id="@+id/customerGroupLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvTitleCustomerGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center|top"
            android:layout_marginTop="@dimen/fragment_title_magin_top"
            android:text="@string/title_fragment_customer_group"
            android:textColor="@color/cyan"
            android:textSize="30sp" />

        <ListView
            android:id="@+id/lvAddCustomerGroup"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />
    </LinearLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fabAddCustomerGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_marginBottom="@dimen/fab_add_customer_margin_bottom"
        android:layout_marginEnd="@dimen/fab_add_customer_margin_right"
        android:layout_marginRight="@dimen/fab_add_customer_margin_right"
        android:src="@drawable/ic_fab_add_custom"
        app:borderWidth="0dp" />

</android.support.design.widget.CoordinatorLayout>